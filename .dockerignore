# Keep the build context lean: deps are reinstalled with `npm ci`, and the
# build output is regenerated inside the image. rootfs/ IS needed (it's COPYied).
node_modules
dist
.git
.env
*.log
.vite
tsconfig.tsbuildinfo
model-source
