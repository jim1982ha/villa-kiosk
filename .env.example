# Villa Kiosk — environment defaults
# Copy to ".env" and fill in. All values are OPTIONAL: anything left blank can
# be entered at runtime in the Onboarding wizard / Settings. The token is best
# entered in the app rather than baked into the build.

# --- Home Assistant connection (Q3) ---
VITE_HA_URL=http://homeassistant.local:8123
VITE_HA_PORT=8123
# VITE_HA_TOKEN=eyJhbGciOi...   # long-lived token (optional, not recommended in builds)

# --- Villa location for sun tracking (Bali / TheLysHouse) ---
VITE_LAT=-8.3405
VITE_LNG=115.0920

# --- Deployment target for `npm run deploy` (Q5) ---
# SSH/SCP target (NOT the web URL): no http:// and no :8123. HA OS has no SSH
# server by default — install the "Advanced SSH & Web Terminal" add-on. Set
# VITE_DEPLOY_PORT only if that add-on doesn't listen on 22.
VITE_DEPLOY_HOST=homeassistant.local
VITE_DEPLOY_USER=root
VITE_DEPLOY_PATH=/config/www/villa-kiosk
# VITE_DEPLOY_PORT=22
