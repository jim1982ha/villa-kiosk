"""The deterministic briefing — the layer a future cutover DELETES WHOLESALE.

The owner's words (2026-08-28): "in the future we may completely scratch and
remove all the files that were previously handling the legacy way of
monitoring the villa." That future is now a statement about THIS folder: the
pipeline, its registry gating (including the one supervision-conditional
branch left anywhere, `registry.gate`'s stand-down), the narration adapters
and the task reconciliation go together; `shared`, `adapters` and `supervise`
stay. Imports only shared + adapters; `supervise` never imports this — the
host wires the two at boot (set_concerns_source / set_*_composer), which is
the inversion that has kept ARCH-003 true all along.
"""
