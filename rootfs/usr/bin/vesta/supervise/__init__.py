"""The agent and its observation floor — the layer a future EXPORT ships.

The owner's words (2026-08-28): "the future possibility for us to export the
Agent and deploy all of it in an external resource... this should remain a
capability." That future is a statement about THIS folder plus `vesta.shared`:
ship both, write one `vesta.adapters` against a remote HA and store, start the
loops, mount the routes. `supervise` never imports `brief` — the host wires the
two at boot — and test_layering.py enforces it.
"""
